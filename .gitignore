# =============================================================================
# One Link Website  -  .gitignore
# =============================================================================

# Rust / Cargo build artifacts.
/live/wasm/target/
**/*.rs.bk
Cargo.lock

# Coherence Lang transpile cache (regenerable from .cl source).
.cl_build/

# Wrangler local state.
.wrangler/
.dev.vars
.dev.vars.*

# Node / npm if anything ever sneaks in (it shouldn't).
node_modules/
package-lock.json
npm-debug.log*

# Python bytecode caches.
__pycache__/
*.pyc
*.pyo

# Editor / OS.
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Logs.
*.log

# Secrets - never commit.
.env
.env.*
*.key
*.pem
*-private*

# Offline ed25519 signing key for /manifest.json. ONLY the public half
# is committed (pinned as a constant in dist/weareone-link.org/sw.js).
# The private key lives ONLY in .keys/manifest-ed25519.sk on this box.
.keys/

# Optional local-only dist staging (real dist/ DOES get committed so the
# Worker can deploy the prebuilt assets without a CI build step).
/dist-local/
staging/

# Release binaries + source archives: served from R2 in production. The
# 38 MB source tarballs do not belong in the website git history.
dist/weareone-link.org/downloads/*.tar.gz
dist/weareone-link.org/downloads/*.zip
dist/weareone-link.org/downloads/*.exe
dist/weareone-link.org/downloads/*.bin
dist/weareone-link.org/downloads/*.dmg
dist/weareone-link.org/downloads/*.apk
